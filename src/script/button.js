// Button behaviour
const CreateCardElement = () => {
    // Function when button is clicked.
    const Button = document.getElementById('createCard')

    const button = console.log(`Button clicked, func status: ${CreateCardElement}`)
}
